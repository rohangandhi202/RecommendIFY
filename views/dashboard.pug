extends layout.pug
    
block content
    h1 Welcome #{user.display_name}! 
    h2 Here are your 20 most recently liked songs
    h2 Click on a song to see song recommendations
    h2 Click on an artist link to see similar artists 
    img(src=user.images[0].url, class='circular_image')
    
    table 
      each t in tracks
        tr 
          - var artist_name = t.track.artists[0].name
          - var track_name = t.track.name
          - var track_preview = t.track.preview_url
          - var id = t.track.artists[0].id
          - var track_id = t.track.id 

          td: a(href='/artists/2jku7tDXc6XoB6MO2hFuqg/related-artists') #{artist_name}
          td: a(href='/recommendations?artist=' + artist_id+'&track='+track_id) #{track_name}
          td: a(href=track_preview) Preview